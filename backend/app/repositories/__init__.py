"""Data-access layer: queries and transactions behind repository interfaces.

Phase-1 placeholder. The only layer allowed to import ``models`` and
``database`` directly; services depend on these interfaces, not SQL.
"""
