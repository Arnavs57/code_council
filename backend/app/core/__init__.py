"""Domain kernel: exceptions, constants, responses and helpers.

``core`` is the bottom of the dependency graph — no other application package
may be imported from here. Everything else may import from ``core``.
"""
