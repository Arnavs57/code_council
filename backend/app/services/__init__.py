"""Application services: non-AI business logic (runs, findings, policies).

Phase-1 placeholder. Services orchestrate repositories and are free of HTTP
and FastAPI imports so they are testable in isolation.
"""
