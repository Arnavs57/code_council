"""Generic, domain-agnostic utilities (time, ids, serialization).

Note: the application-wide constants, exceptions, responses and helpers live
in ``app/core`` (the domain kernel). This package is reserved for helpers
that have no domain semantics.
"""
